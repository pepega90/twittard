let Hooks = {}

Hooks.AddTweet = {
    mounted() {
        // this.el.addEventListener("submit", (e) => {
        //     e.preventDefault();
        //     let tweetVal = e.target.querySelector("input[name='tweet']").value;
        //     this.pushEvent("add", {tweet: tweetVal})
        //     e.target.querySelector("input[name='tweet']").value = "";
        // })
    }
}

export default Hooks;